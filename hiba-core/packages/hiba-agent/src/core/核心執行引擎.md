---
type: folder-note
---

# 核心執行引擎

工具定義（`defineTool`）、工具箱（`HiBAToolbox`）、作用域工具箱（`ScopedToolbox`）、HTTP 節點能力查詢（`HttpNodeCapabilityChecker`）與資源決策服務（`ResourceDecisionService`）的實作。遵循三大公理：工具同構（A1）、權限遞減（A2）、資源決策四分支（A3）。
