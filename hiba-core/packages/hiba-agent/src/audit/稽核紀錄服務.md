---
type: folder-note
---

# 稽核紀錄服務

`AuditTrail`：實作 `AuditWriter` 介面，以 better-sqlite3 寫入不可竄改的執行稽核記錄（T2 定理：每次執行無論成功或失敗皆產生 `AuditRecord`）。支援批次上鏈（`batchUploadToChain`）與條件查詢（`query`），寫入失敗統一拋出 `AUDIT_ANCHOR_FAILED`。
